# node_modules

1. its is used to install all external package and libraries in react js 
2. node_modules is also run the script of react js 
3. npm install 

# public 

1. all the asset file are here .. and this is accessible global 

**asset**

1. asset => images
=> css
=> js
=> fonts


# src 
1. all the components are created here of react js 
2. all assets created in this images | js | css 
3. components is a small peace of file that can be saved with .js or .jsx (javascript xml) that is called components 
4. components are re-usable 
5. when we create app inside of react js using vite package its components is always saved with .jsx 


# index.html 

  1. this is our home page 
  2. index.html is called inside of react js via virtual DOM 
     examples . document.getElementById();


# package.json 

  1. package.json is  

     1. package.json file is environment file that is in form of .json 
     2. all the information about app are registered here



# package-lock.json

   1. samples of package.json 
   

